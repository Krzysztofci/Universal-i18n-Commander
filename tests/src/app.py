# Sample source file referencing some i18n keys
print(_('hello'))
# "bye" appears as a literal in code but not as function
some_var = "This is a string with bye inside"
# another usage: single quotes
label = _('dup1')
